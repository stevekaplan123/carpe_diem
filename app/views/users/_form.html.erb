<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
<!--   <div class="field">
    <%= f.label :num_events %><br>
    <%= f.number_field :num_events %>
  </div>
  <div class="field">
    <%= f.label :geo_info %><br>
    <%= f.text_field :geo_info %>
  </div> -->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
